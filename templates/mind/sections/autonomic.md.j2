## 自律神経バランス

> HRVは副交感神経活動の指標。高いほどリラックス・回復状態。

{% if autonomic.hrv_stats %}
### HRV (心拍変動)

| 指標 | 値 |
|------|-----|
| 平均RMSSD | {{ autonomic.hrv_stats.avg_rmssd }}ms |
| 変動幅 (SD) | {{ autonomic.hrv_stats.std_rmssd }}ms |
| 範囲 | {{ autonomic.hrv_stats.min_rmssd }} 〜 {{ autonomic.hrv_stats.max_rmssd }}ms |
| 開始→終了 | {{ autonomic.hrv_stats.first_rmssd }} → {{ autonomic.hrv_stats.last_rmssd }}ms ({{ autonomic.hrv_stats.change_rmssd }}) |
{% endif %}

{% if autonomic.rhr_stats %}
### 安静時心拍数

| 指標 | 値 |
|------|-----|
| 平均RHR | {{ autonomic.rhr_stats.avg_rhr }}bpm |
| 範囲 | {{ autonomic.rhr_stats.min_rhr }} 〜 {{ autonomic.rhr_stats.max_rhr }}bpm |
| 開始→終了 | {{ autonomic.rhr_stats.first_rhr }} → {{ autonomic.rhr_stats.last_rhr }}bpm ({{ autonomic.rhr_stats.change_rhr }}) |

> HRV上昇 & RHR低下 = 回復良好、HRV低下 & RHR上昇 = ストレス/疲労
{% endif %}
