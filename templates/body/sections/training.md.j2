## 🏋️ トレーニング

{% if aerobic_data %}
> 心拍ゾーン（%HRR カルボーネン法, maxHR=Tanaka{{ hr_zone_meta.max_hr }} / RHR{{ hr_zone_meta.resting_hr|number_format(0) }}）
> 境界(bpm): Z1≥{{ hr_zone_meta.bounds.Z1|number_format(0) }} / Z2≥{{ hr_zone_meta.bounds.Z2|number_format(0) }} / Z3≥{{ hr_zone_meta.bounds.Z3|number_format(0) }} / Z4≥{{ hr_zone_meta.bounds.Z4|number_format(0) }} / Z5≥{{ hr_zone_meta.bounds.Z5|number_format(0) }}
{% if zone2_meta %}
> **Zone2**(いわゆる有酸素ベース/LT1直下): LT1={{ zone2_meta.lt1|number_format(0) }}bpm（{{ zone2_meta.lt1_source }}{% if zone2_meta.lt1_source == 'maf' %}=180-{{ zone2_meta.age }}・暫定/低体力者は過大傾向{% endif %}）, Zone2帯=[{{ zone2_meta.band_lower|number_format(0) }}, {{ zone2_meta.band_upper|number_format(0) }}) bpm
{% endif %}

| 日付 | 歩数 | Zone合計 | Z1 | Z2 | Z3 | Z4 | Z5 | Zone2 |
|------|------|----------|----|----|----|----|-----|-------|
{% for row in aerobic_data %}
| {{ row.date | date_format }} | {{ row.steps | number_format(0) }} | {{ row.total_zone_min | number_format(0) }} | {{ row.z1_min | number_format(0) }} | {{ row.z2_min | number_format(0) }} | {{ row.z3_min | number_format(0) }} | {{ row.z4_min | number_format(0) }} | {{ row.z5_min | number_format(0) }} | {{ row.zone2_min | number_format(0) }} |
{% endfor %}
{% if vo2max_final %}
> VO2 Max: 計測終了（最終計測 {{ vo2max_final.date | date_format }} / {{ vo2max_final.vo2_max | number_format(0) }}）
{% endif %}
{% endif %}

### 💪 筋トレ

{% if strength_data %}
| 日付 | 回数 | 時間(分) | 平均HR | kcal |
|------|------|----------|--------|------|
{% for row in strength_data %}
| {{ row.date | date_format }} | {{ row.count }} | {{ row.duration | number_format(0) }} | {{ row.avg_hr | number_format(0) }} | {{ row.calories | number_format(0) }} |
{% endfor %}{% else %}
> 記録なし
{% endif %}

### 🚴 サイクリング

{% if cycling_data %}
| 日付 | 回数 | 時間(分) | 平均HR | kcal |
|------|------|----------|--------|------|
{% for row in cycling_data %}
| {{ row.date | date_format }} | {{ row.count }} | {{ row.duration | number_format(0) }} | {{ row.avg_hr | number_format(0) }} | {{ row.calories | number_format(0) }} |
{% endfor %}
{% else %}
> 記録なし
{% endif %}
