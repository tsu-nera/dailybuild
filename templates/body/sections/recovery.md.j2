{% if recovery_data %}
## 🛌 回復

| 日付 | 睡眠(h) | 深い(m) | HRV(ms) | HR(bpm) | 判定 |
|------|---------|---------|---------|---------|------|
{% for row in recovery_data %}
| {{ row.date | date_format }} | {{ row.sleep_hours | number_format(1) }} | {{ row.deep_minutes | number_format(0) }} | {{ row.hrv | number_format(0) }} | {{ row.hr | number_format(0) }} | {% if row.training_intensity == 'high' %}🟢{% elif row.training_intensity == 'medium' %}🟢{% elif row.training_intensity == 'low' %}🟡{% else %}⚪{% endif %} {{ row.training_recommendation or '-' }} |
{% endfor %}
{% endif %}
